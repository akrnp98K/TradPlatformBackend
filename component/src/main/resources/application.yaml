spring:
  application:
    name: component
  datasource:
    username: root
    password: Xjy123456
    url: jdbc:mysql://localhost:3306/traderplatform
    driver-class-name: com.mysql.cj.jdbc.Driver
    # druid ?????
    druid:
      # ?????????????
      initial-size: 5
      # ??????????
      min-idle: 5
      # ?????????
      max-active: 20
      # ????????????????
      max-wait: 60000
      # ??????????????????timeBetweenEvictionRunsMillis???validationQuery?????????
      test-while-idle: true
      # ?????????????testWhileIdel?????
      time-between-eviction-runs-millis: 60000
      # ????????????????????????????????????(??????????????)
      min-evictable-idle-time-millis: 30000
      # ??????????????sql ?????????(oracle?? select 1 from dual)
      validation-query: select 'x'
      # ????????validationQuery????????,???????,???true
      test-on-borrow: false
      # ????????validationQuery????????,???????,???true
      test-on-return: false
      # ????preparedStatement, ???PSCache,PSCache???????????????????oracle,?mysql??????
      pool-prepared-statements: false
      # ????????filters????????sql?????stat: ?????Slf4j:?????waLL: ??sqL??
      filters: stat,wall,slf4j
      # ???PSCache???????0????0??poolPreparedStatements???????true??Druid??????Oracle?PSCache??????????????????????????100
      max-pool-prepared-statement-per-connection-size: -1
      # ????DruidDataSource?????
      use-global-data-source-stat: true


mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    com.xiaojingye.wechatbackend.component.mapper: debug
